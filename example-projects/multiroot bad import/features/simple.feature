Feature: Bad import

   Scenario: scenario that fails due to bad import
      Given we have behave installed
      When we implement a successful test
      Then we will see the result

   Scenario: another scenario that should also fail
      Given we have behave installed
      When we implement a successful test
      Then we will see the result


