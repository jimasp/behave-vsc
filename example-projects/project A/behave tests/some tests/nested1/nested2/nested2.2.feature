Feature: Nested 2.2

   Scenario: failure
      Given we have behave installed
      When we implement a failing test
      Then we will see the result

   @skip
   Scenario: skipped
      Given we have behave installed

