# this project is here to test handling of a bad (as opposed to unconfigured) features path

on startup (or test explorer refresh), it should show a warning to update the behave configuration "paths" setting