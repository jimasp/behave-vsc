Feature: Bad import

   Scenario: a scenario that should either not run (due to cancel) or fail 1
      Given we have behave installed
      When we implement a successful test
      Then we will see the result

   Scenario: a scenario that should either not run (due to cancel) or fail 2
      Given we have behave installed
      When we implement a successful test
      Then we will see the result

   Scenario: a scenario that should either not run (due to cancel) or fail 3
      Given we have behave installed
      When we implement a successful test
      Then we will see the result

   Scenario: a scenario that should either not run (due to cancel) or fail 4
      Given we have behave installed
      When we implement a successful test
      Then we will see the result                  

   Scenario: a scenario that should either not run (due to cancel) or fail 5
      Given we have behave installed
      When we implement a successful test
      Then we will see the result
